# Embedded Note Image Sync Review Remediation

> Historical review input: this document records findings against the removed
> feature-v2 stage/recovery implementation. The current runtime architecture is
> the explicit schema-v3 state machine documented in
> `embedded-note-image-sync-design.md`; names such as `recoverTransaction()` and
> `old-delete-confirmed` below are not current production paths.

This document maps every independent-review finding against commit
`fe7300ebf665c42d965bc8550775193eaf73e10c` to a reproducible regression test
and the smallest safe implementation change. It was written before changing
the implementation.

## Finding-to-test matrix

| ID  | Affected files and symbols                                                                                                                                | Reproducible failure                                                                                                                                                                | Failing test to add first                                                                                                                                                                                    | Minimum repair                                                                                                                                                                                                  | Acceptance after repair                                                                                                                                   |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1  | `item-data.ts`: `SyncedNote`, `SyncedNotes`; `sync-note-item.ts`: `recoverNoteState()`, `deleteBlockWithVerification()`, `getEffectiveContainerBlockID()` | Syntactically valid metadata points at a user block, another note/container/page, or a block made by another bot, and the current code deletes, promotes, or adopts it by ID alone. | `note-block-ownership.spec.ts` and coordinator cases for hostile active/candidate/orphan/container IDs and legacy IDs.                                                                                       | Add versioned remote ownership markers bound to target, library, parent, note, kind, and attempt; verify marker, parent, type, page, and creator before every mutation. Quarantine unverifiable legacy state.   | No bare ID authorizes delete/archive/promote/restore/adoption; unrelated blocks receive no mutation request.                                              |
| B2  | `html-to-notion.ts`: `convertRichTextChildNodes()`, `convertRichTextNode()`, `convertParentElement()`; `sync-note-item.ts`: block preparation             | Images inside anchors, spans, strong text, headings, or nested inline wrappers are found but silently converted to empty rich text.                                                 | Parser-to-block cases for every reviewed wrapper/order plus coordinator count-mismatch rejection.                                                                                                            | Make ordered inline segments first-class, normalize inline images to block boundaries, and compare discovered/resolved/prepared/rendered occurrence counts before candidate creation.                           | Every supported discovered image produces exactly one image block in DOM order; unsupported nesting aborts before replacement.                            |
| H1  | `item-data.ts`; `sync-note-item.ts`: create/append/finalize/commit/recovery flow                                                                          | Process loss between remote writes and metadata saves loses container/candidate identity or promotes a partial candidate.                                                           | Restart simulations at container-create response loss, candidate creation, partial/full append, title update, candidate save, old deletion, promotion, and orphan cleanup.                                   | Persist a versioned transaction and stable attempt ID before the first remote write; persist each stage/remote ID; reconcile only exact remote markers after restart.                                           | Restart deterministically resumes, rolls back, or quarantines without deleting unknown blocks, duplicating indefinitely, or promoting incomplete content. |
| H2  | `sync-note-item.ts`: `blockExistsWithRetry()`, `deleteBlockWithVerification()`                                                                            | A retrieve/delete 404 caused by missing sharing is treated as proof that a block is absent/deleted.                                                                                 | Permission-hidden 404 and indistinguishable true-absence cases after a lost delete response.                                                                                                                 | Treat 404 as `uncertain`; accept deletion only from a successful delete response whose returned block has `in_trash: true`.                                                                                     | Candidate remains recoverable and active mapping is preserved whenever deletion cannot be positively confirmed.                                           |
| H3  | `notion-image-upload-service.ts`; `item-data.ts`; `sync-note-item.ts`: `prepareImages()`                                                                  | Successful uploads are forgotten when a later upload/candidate/append fails; create/send ambiguity can duplicate bytes or upload objects.                                           | Partial multi-image failure followed by retry, candidate failure followed by retry, create-response loss reconciliation, ambiguous/multiple list matches, send ambiguity, expiry, and restart during upload. | Add a provisional upload journal bound to complete source/target/attempt identity; persist IDs/status immediately; reconcile ambiguous create through `fileUploads.list()` and ambiguous send through retrieve. | Known valid uploads are reused, expired/failed uploads are rejected, and ambiguous outcomes fail safely without blind replay.                             |
| H4  | `sync-note-item.ts`: `getEffectiveContainerBlockID()`                                                                                                     | Moving note A under a user toggle replaces the global container ID, so first sync of note B writes below that user block.                                                           | Sequential note-A-moved, note-A-sync, note-B-first-sync coordinator test.                                                                                                                                    | Verify one canonical container independently; treat a moved note location as local/unverified and rebuild under the canonical container without mutating the moved block.                                       | Note B always lands in the verified canonical container and a user block never becomes the sync root.                                                     |
| H5  | `item-data.ts`: `getSyncedNotesFromAttachment()`, `saveSyncedNoteRecord()`                                                                                | One malformed image/candidate/orphan/transaction field marks every note corrupt and permanently blocks safe progress; evidence is overwritten or lost.                              | Partial corruption, future fields, old schema, one bad note beside one good note, and corrupt optional subrecord tests.                                                                                      | Add an explicit schema version and per-note parsing; quarantine only unsafe fields/records while retaining verified active mappings and a redacted diagnostic snapshot/hash.                                    | Healthy notes continue, corrupt fields never authorize mutation, and users receive a recoverable error with preserved evidence.                           |
| M1  | `note-image-resolver.ts`: hashing/text encoding; `notion-image-upload-service.ts`: `Blob`; `sync-job.ts`/`notion-client.ts`: realm wiring                 | Node/jsdom globals make tests pass although Zotero bootstrap can reject cross-realm Blob/FormData/fetch/crypto objects.                                                             | Realm adapter test whose constructors reject foreign objects and whose crypto/random UUID are spies from one window realm.                                                                                   | Introduce one injectable Zotero main-window Web API adapter and use its Blob, TextEncoder/Decoder, crypto, random UUID, fetch, and FormData-compatible SDK path.                                                | Multipart construction and cryptography use a mutually compatible Zotero realm; real Zotero 10 smoke test remains manual.                                 |
| M2  | `notion-image-upload-service.ts`; `sync-note-item.ts`: status/reconciliation retry helpers                                                                | 429 ignores `Retry-After`; all retryable statuses use fixed tiny delays; total wait is not bounded; ambiguous writes are not classified.                                            | Injectable sleeper/clock tests for integer/date/invalid `Retry-After`, 409/529/5xx jittered backoff, 401/403 no retry, max attempts, and max total wait.                                                     | Centralize bounded retry policy with request safety classes, jitter, `Retry-After`, elapsed-time cap, and reconciliation for ambiguous mutations.                                                               | Delays and attempt counts are deterministic in tests, bounded in production, and no ambiguous mutation is blindly replayed.                               |
| M3  | `sync-job.ts`: `prepareSyncJob()`; `sync-note-item.ts`: disabled path; `item-data.ts`                                                                     | Feature OFF still calls image-only `users.me()`, calculates image metadata/hash fields, and persists `images: []`/target state unlike `main`.                                       | Main-vs-feature request/mapping regression with IMG HTML while OFF and a `users.me()` gate test.                                                                                                             | Gate workspace-limit lookup and all image parsing/resolution/upload/journal fields behind the preference; keep text-only mapping free of image-only fields.                                                     | OFF performs no attachment/file/upload/user-limit work and retains the closest safe text-only request/mapping shape.                                      |
| M4  | `note-image-resolver.ts`: `validateImageBytes()`, `matchesMimeType()`; resolver fixtures                                                                  | Header-only/truncated files pass; SVG regex accepts unsafe or malformed XML and external resources.                                                                                 | Real minimal PNG/JPEG/GIF/WebP fixtures plus truncated, forged MIME, XML-declaration SVG, malicious SVG, and valid SVG cases.                                                                                | Structurally validate raster containers; parse SVG XML, require an SVG root, and reject scripts, event handlers, and external references (or reject SVG if safe parsing is unavailable).                        | Only structurally valid supported images proceed; unsupported/corrupt content aborts with the old note intact.                                            |
| M5  | `sync-note-item.ts`: `resolveEmbeddedImages()`, `prepareImages()`; docs/locales                                                                           | The note has no image-count or aggregate-byte cap and retains every image byte array before serial upload.                                                                          | Count-limit, aggregate-byte-limit, serial ordering, and byte-lifetime/reload tests.                                                                                                                          | Preflight serially to metadata/hashes, enforce per-note limits, then reload/verify/upload one image at a time with concurrency one and prompt byte release.                                                     | Limits are localized/documented; memory is bounded to one image payload and uploads remain ordered/serial.                                                |

## H6 CI gate

H6 is handled as a delivery gate rather than a runtime change. After the
remediation commit is pushed to the existing branch, checks and workflow runs
must be queried for that exact SHA. A missing or disabled workflow is reported
as not run, never as passed. No workflow or quality gate will be weakened to
manufacture a green result.

## API constraints used by the repair

- The installed Notion SDK exposes `fileUploads.list()` for uploads owned by
  the current bot connection.
- An uploaded File Upload must be attached before its expiry; after attachment
  its expiry is cleared and the ID remains reusable, including after deleting
  the original block.
- A Block response exposes its parent, block type, creator, and trash status.
- A 404 does not provide enough evidence to distinguish absence from content
  hidden by connection permissions, so it is never positive deletion proof.

## Second independent review finding-to-test matrix

This matrix covers the targeted follow-up review against
`0c2bf882d1bec98e6fd8c4d9b5a02af2f1581a69`. It was recorded before changing
production code for this remediation round.

| ID   | Current failure path                                                                                                                                                                       | Failing regression test added first                                                                                                                                                                                          | Minimum architecture repair                                                                                                                                                                                                                                               | Success criterion                                                                                                                                                                                       | Remote tree and metadata after crash/restart                                                                                                                                                                                                                                               |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| H-01 | `NotionImageUploadService.createSafely()` retries 409/429/5xx create writes, and timeout-only reconciliation treats zero matches as a terminal failure without a durable isolation window. | Stateful create injections for 500, 503, 504, and 529 where the server creates the upload but the client receives an error; zero-match-before-deadline and multiple-match reconciliation cases.                              | Classify create outcomes, persist a complete upload attempt before create, reconcile every result-uncertain create through bounded `fileUploads.list()`, and allow another create only after the persisted isolation deadline expires with a confirmed zero-match result. | One create per live attempt; exactly one trusted candidate may be claimed; zero and multiple matches never trigger an immediate replacement create.                                                     | The fake server may contain one unattached upload; metadata remains `create-uncertain` with the original attempt/request window/deadline. Restart resumes list reconciliation, keeps the active note, and does not create again before expiry.                                             |
| H-02 | `onCreated`/`onStatus` or metadata persistence errors are caught by a generic upload catch and can overwrite recoverable state with `failed`; known upload IDs can be lost.                | Journal-save failures before create, immediately after create, after send status, after zero/multiple reconciliation, plus JSON round-trip and full service/coordinator restart; assert create/send counts remain unchanged. | Introduce typed remote-uncertain, journal-persistence, and reconciliation-ambiguous errors; require pre-write journal durability; keep the newest in-memory journal evidence without overwriting the last durable snapshot; reconcile known IDs with retrieve.            | Persistence failure can never authorize create/send replay, and typed errors preserve the last recoverable evidence and actionable stage.                                                               | The remote upload remains pending/uploaded as actually written; the last durable journal remains byte-for-byte intact. Restart uses the durable attempt plus deterministic list/retrieve reconciliation and preserves the old active note.                                                 |
| H-03 | `recoverTransaction()` rejects any `transaction.sourceHash` different from the current note hash, permanently blocking changed text/images or an ON/OFF preference transition.             | Crash with an old pending attempt, then change text, delete/replace the failing image, toggle ON→OFF and OFF→ON, JSON round-trip, restart, and complete the new source; assert the old attempt is never promoted.            | Recover or isolate the old attempt solely from its persisted source/target/ownership/attempt/candidate/orphan/upload journal; after bounded cleanup or quarantine, start a new transaction for the current source.                                                        | Source changes do not require old HTML or old files and do not promote old content; current content can converge after safe old-attempt handling.                                                       | A verified old candidate is trashed or retained as a bounded orphan; an unverified one is never mutated. The active mapping stays last-known-good until a fresh transaction commits, after which metadata points only to the new active note plus any explicit unverified orphan evidence. |
| H-04 | `orphan-cleanup` is readable in recovery but normal failure handling never durably enters it, and cleanup is saved only after all deletions.                                               | Real chain: append failure → candidate cleanup failure → metadata JSON round-trip → destroy coordinator/services/locks → restart → bounded cleanup success → unchanged resync fast path.                                     | Atomically persist `stage: orphan-cleanup`, owned orphan references, attempt/target, and bounded retry counters on cleanup failure; verify and remove one bounded batch at a time, persisting progress after each deletion.                                               | Every production-written orphan state is recoverable; unverified blocks are not deleted; completion clears both orphan list and transaction and unchanged sync performs no rebuild/upload.              | Partial candidates are either trashed after verified cleanup or remain explicitly unverified. Metadata monotonically removes confirmed-deleted orphans; after completion it contains one active mapping, no transaction/orphans, and restart fast-paths.                                   |
| H-05 | Main-format `containerBlockID`/`noteBlockIDs` are labeled legacy-unverified but synchronization stops permanently instead of providing a non-destructive migration.                        | Real main metadata with one/multiple notes, image feature ON/OFF, legacy IDs that resolve to user blocks or 404, migration restart, and second sync; assert no legacy delete/update/archive request.                         | Keep legacy IDs as immutable evidence, create a new marked canonical container and managed copies, then save v2 active mappings only after complete copy; persist a one-time migration notice/state so repeated sync cannot copy forever.                                 | Legacy blocks are never adopted or mutated; text-only OFF and image ON can both create a safe canonical copy; user receives an explicit duplicate/cleanup notice.                                       | Legacy remote content remains unchanged regardless of accessibility. Metadata retains legacy references as unmanaged evidence and points active v2 mappings at the complete marked copies; restart/unchanged sync creates no additional copy.                                              |
| M-01 | Full ASCII ownership markers are visible gray code text in headings; verification and recovery use substring matching.                                                                     | Request/response model tests proving marker identity is absent from `plain_text`; exact-token tests for prefix/suffix/href changes, deletion, title edits, and Notion rich-text segmentation/merging.                        | Encode one invisible U+2063 text run per marker with the complete marker URL in `text.link.url`; centralize exact extraction/verification and use it for retrieve, list reconciliation, delete, promote, cleanup, and adoption decisions.                                 | Normal heading `plain_text` contains only the user-facing title plus invisible separators; only an exact unique rich-text content/link pair verifies ownership.                                         | Altered or missing marker runs make ownership unverified and block mutation. Valid blocks remain discoverable after restart by the exact link token without exposing ASCII identity to title/search/AI text.                                                                               |
| M-02 | PNG validation omits CRC/APNG checks, JPEG returns true at SOS without validating scan completion, and SVG accepts a broad sanitizer surface.                                              | Real decodable PNG/JPEG/GIF/WebP fixtures plus PNG CRC corruption/truncation/APNG, truncated JPEG scan, malicious/XML SVG, AVIF/BMP, MIME mismatch, and injected decoder failure.                                            | Remove SVG support for this RC; implement PNG chunk/CRC/IEND/APNG validation, full JPEG marker/scan validation, structural GIF/WebP checks, and an injected Zotero main-window image decoder adapter.                                                                     | Corrupt, animated/unsupported, mismatched, or decoder-rejected bytes fail before upload and leave the old note intact; supported fixtures decode successfully.                                          | No remote mutation or image metadata write occurs for rejected bytes. Restart sees the prior valid active mapping and no upload journal for the rejected image.                                                                                                                            |
| M-03 | `schemaVersion` values above v2 are parsed as usable records and later silently written back as v2.                                                                                        | Version-99 parse/sync/save/JSON-round-trip tests asserting the original metadata string is unchanged and every Notion mutation count is zero.                                                                                | Represent future schema as an explicit unsupported read-only state carrying the original serialized metadata; reject sync and save before any remote write or attachment mutation.                                                                                        | Future metadata is never downgraded or normalized and reports an actionable upgrade-required error.                                                                                                     | Remote tree is untouched; the exact original metadata bytes remain persisted across restart and repeated sync attempts.                                                                                                                                                                    |
| M-04 | Coordinator tests use call-count mocks that do not model block order, trash state, upload lifecycle, durable JSON, or real restart boundaries.                                             | Reusable stateful fake covering H-01–H-05 core paths, 100-block batching, ambiguous middle append, multiple notes/parents/targets, legacy, feature OFF, future schema, and orphan cleanup.                                   | Add an in-memory Notion server/client with real page/block trees, ownership, ordering, trash, upload create/send/retrieve/list, failure/result-uncertain injection, and a metadata harness that serializes/deserializes JSON between new coordinator instances.           | Tests assert converged state, not only calls: one complete active note, preserved user blocks, ordered text/images, idempotent uploads, consistent mapping, and no partial candidate treated as active. | Every injected crash destroys local instances and reloads only serialized metadata while server state persists. Assertions compare the recovered remote tree and journal/mapping after convergence and verify no pending async work remains.                                               |

## Final independent review finding-to-test matrix

This matrix covers the targeted repair against
`f08e07eba12647e1d154daafc183c21bb0a66b29`. It was recorded before adding
the failing tests or changing production code for this repair round.

| ID    | Production failure path                                                                                                                                                                                                              | Failing test added first                                                                                                                                                                                                     | Safety invariant                                                                                                                                                                                                                                   | Minimum repair                                                                                                                                                                                                                                                       | Final state after JSON restart                                                                                                                                                                                                                                              |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-H01 | `recoverSupersededTransaction()` deletes a complete candidate when its transaction is `old-delete-confirmed` but the current Zotero source hash changed; a later upload/append failure can therefore leave no complete visible note. | Three stateful restart chains from `old-delete-confirmed`: changed-source upload failure, changed-source later-batch append failure, and changed-source success.                                                             | Once the previous active was positively deleted, the complete candidate is the sole last-known-good version and cannot be treated as an incomplete or superseded candidate.                                                                        | Verify the persisted complete candidate and its stable ownership marker, promote it under the old transaction source hash without using current source HTML/title, then use it as the previous active for a fresh current-source transaction.                        | Failure preserves the old candidate as the visible active mapping with its old source hash; success deletes it only after the fresh candidate is complete and leaves exactly one current active note.                                                                       |
| F-H02 | A File Upload create returns an ID, `onCreated` persistence fails, and restart reconciliation finds one pending upload, but the coordinator persists `pending` and stops forever instead of sending the already-created upload.      | Full stateful create-ID/journal-failure/restart/list/send/attach/commit convergence, repeated persistence failure without another create, and send-success/status-save failure with retrieve-only recovery.                  | A known create ID is never discarded or downgraded to a state that authorizes another create; bytes are sent only after `created-unsent` is durable, and `send-uncertain` is never resent.                                                         | Use explicit `prepared`, `create-uncertain`, `created-unsent`, `send-uncertain`, `uploaded`, `attached`, `failed`, and `expired` states; retry local journal writes boundedly; resume unique pending reconciliation with `sendCreated()`.                            | Durable pre-create evidence remains unchanged when created-ID persistence exhausts its retry budget; later unique reconciliation durably records `created-unsent`, sends exactly once, retrieves as needed, attaches, and commits.                                          |
| F-H03 | Container/candidate 401/403 leaves `*-create-uncertain`; zero-match reconciliation has no same-source exit and can permanently block a note; the container create marker is not attempt-specific.                                    | Stateful container and candidate cases for proven-unexecuted auth errors, timeout/zero match before and after a persisted deadline, timeout/one exact remote match, and multiple exact matches.                              | Proven-unexecuted requests roll back without remote cleanup; result-uncertain requests retain a stable attempt marker and cross-restart deadline; only one exact creator/parent/marker match may be adopted, and unknown blocks are never deleted. | Persist `createUncertainUntil`; make container and candidate create markers attempt-specific; classify block-create outcomes; perform bounded strict reconciliation; after-deadline exhaustive zero match clears the attempt and allows a later fresh create.        | Auth repair retries safely. Before deadline, zero match preserves the attempt without another create. After a final zero match, metadata rolls back to a safe retryable state. One exact match is adopted; multiple matches remain untouched and produce a recovery error.  |
| F-M01 | An image in append batch 1 becomes persistent remotely, batch 2 fails, candidate cleanup succeeds, and the provisional cache still expires the upload locally; retry after one hour uploads the same bytes again.                    | Stateful batch-1-image/batch-2-failure/candidate-cleanup/clock-advance/restart/retry test asserting unchanged create/send counts, plus uncertain-append attachment reconciliation.                                           | Every definitely attached upload is permanent reusable evidence, independent of candidate cleanup or its original one-hour upload deadline.                                                                                                        | After each successful append, persist every referenced upload as `attached` with `attachedAt`, `expiryTime: null`, target and content identity; reconcile ambiguous append attachment through retrieve; preserve protected attached mappings during cleanup/pruning. | Candidate deletion does not change the remote attached upload or its durable cache record. After the original expiry and JSON restart, the same source reuses the existing File Upload ID and the replacement can commit without create/send replay.                        |
| F-M02 | Feature OFF plus a legacy manual token has no bot/workspace identity, so `prepareSyncJob()` calls `users.me()` and can block otherwise valid text-only sync on permission, network, or rate-limit failure.                           | Legacy-auth Feature-OFF job/coordinator test with IMG HTML asserting zero `users.me`, File Upload, resolver/read/hash calls and no image metadata; local-identity persistence-failure case asserting no user-block deletion. | Feature OFF adds no image or identity network dependency and never persists raw token/image journal data.                                                                                                                                          | Resolve a stable, explicitly local legacy target identity without a remote request, preferring a persisted random local ID and using a domain-separated token fingerprint only as a non-persisting fallback; never represent it as a real workspace identity.        | Text-only sync remains usable with zero image-only calls. Successful managed metadata contains no image/target/upload fields. Token rotation keeps the persisted local identity stable; a fallback identity mismatch fails closed without deleting prior remote content.    |
| F-M03 | The stateful fake accepts any image upload ID, lets send mutate non-pending uploads, gives create a non-null length by default, expires attached files, and does not model attachment while writing the block tree.                  | Nullable-length reconciliation, missing/pending/failed/expired/wrong-workspace image reference rejection, attach-to-null-expiry, post-delete persistence, delayed reuse, plus the F-H01/F-H02/F-H03 restart chains.          | The fake must reject states the real API rejects and preserve the same ordering/lifetime guarantees that production recovery relies on; every crash reloads only JSON metadata while retaining remote server state.                                | Model `pending`, `uploaded-unattached`, `attached-persistent`, `expired`, and `failed`; validate image references before append; attach on successful write; add an injectable server clock; preserve nullable `content_length`; keep a real ordered block tree.     | Pending/unattached uploads can expire; attached uploads retain `expiry_time: null` even after their block is deleted. List/retrieve preserve nullable fields, and restart tests converge only through production-visible server state rather than retained local instances. |
